# Strings are immutable in Python
# This means that once a string is created, it cannot be changed.
word = "hello"
print(word)  # Output: hello
word = "world"  # This creates a new string object
print(word)  # Output: world